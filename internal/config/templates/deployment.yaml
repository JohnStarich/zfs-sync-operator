apiVersion: apps/v1
kind: Deployment
metadata:
  name: zfs-sync-operator
  labels:
    app: zfs-sync-operator
spec:
  selector:
    matchLabels:
      app: zfs-sync-operator
  template:
    metadata:
      labels:
        app: zfs-sync-operator
    spec:
      containers:
        - name: zfs-sync-operator
          image: "ghcr.io/johnstarich/zfs-sync-operator:{{ .Chart.Version }}"
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        runAsGroup: 1000
        runAsUser: 1000
      serviceAccountName: zfs-sync-operator
